# Environment variables used only when running Jest. Safe to commit — no secrets,
# just test-time config. See jest.config.js for how this gets loaded.

# Suppress pino-http request logs during test runs so `npm test` output stays readable.
# Change to 'debug' temporarily if you need to see request logs while debugging a test.
LOG_LEVEL=silent
